[pytest]
# Exclude data folders that are mounted, protected, or not relevant for unit tests
norecursedirs = compose/data data/clickhouse data compose backup .git node_modules

# Keep test discovery defaults otherwise
